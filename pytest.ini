[pytest]
# Run each sub-project in isolation to prevent module name collisions.
# Use: pytest semantic-mvp/tests/ opa-set-baseline/tests/ opa-derived-baseline/tests/
# Or use the Makefile targets: make test-all
testpaths =
    semantic-mvp/tests
    opa-set-baseline/tests
    opa-derived-baseline/tests
